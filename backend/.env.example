# Backend 环境变量示例。复制为 .env 后按需配置。
# .env 需自行加载（如通过 dotenv 或 IDE 插件），本仓库不自动加载。

# 向量搜索：sqlite-vec 扩展路径（不含 .dylib/.so/.dll），供 memory-store 等组件使用
# 先运行 backend/scripts/setup-vec.sh 安装扩展
# VEC_EXTENSION_PATH=/path/to/backend/extensions/vec0

# Memory 组件 SQLite 文件目录（默认 data/memory）
# MEMORY_BASE_DIR=data/memory

# Web 服务端口（默认 4000）
# PORT=4000
